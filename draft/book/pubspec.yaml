name: isbn_info
description: A Dart command-line application to fetch book information using Google Books API.
version: 1.0.0

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  http: ^0.13.3
  dotenv: ^4.0.0


dev_dependencies:
  lints: ^1.0.0
  test: ^1.16.0
